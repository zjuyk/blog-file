<rss xmlns:atom="http://www.w3.org/2005/Atom" version="2.0">
  <channel>
    <title>Mariadb - 标签 - 千玄洞</title>
    <link>http://localhost:1313/tags/mariadb/</link>
    <description>Mariadb - 标签 | 千玄洞</description>
    <generator>Hugo -- gohugo.io</generator><language>zh-CN</language><managingEditor>ownbyzjuyk@gmail.com (千玄子)</managingEditor>
      <webMaster>ownbyzjuyk@gmail.com (千玄子)</webMaster><copyright>CC BY-NC-SA 4.0</copyright><lastBuildDate>Sat, 09 Nov 2019 01:30:35 &#43;0800</lastBuildDate><atom:link href="http://localhost:1313/tags/mariadb/" rel="self" type="application/rss+xml" /><item>
  <title>开始使用 Mariadb</title>
  <link>http://localhost:1313/posts/mariadb/</link>
  <pubDate>Sat, 09 Nov 2019 01:30:35 &#43;0800</pubDate>
  <author>千玄子</author>
  <guid>http://localhost:1313/posts/mariadb/</guid>
  <description><![CDATA[<p>大多数的数据库管理系统都是商业的，但作为一个重度的Linux使用者来说，开源的DBMS还是更有吸引力。之前一直使用的是 <code>MySQL</code>，但自从被<code>Oracle</code> 收购，就很让人揪心。好在子分支的 <code>MariaDB</code> 能够兼容 <code>MySQL</code> 的命令和操作，加上开源社区的支持力度，让我决定加入<code>MariaDB</code> 的阵营。</p>
<h3 id="1-两者异同" class="heading-element">
  <a href="#1-%e4%b8%a4%e8%80%85%e5%bc%82%e5%90%8c" class="heading-mark"></a>1 两者异同</h3><p>迁移前总得先了解一下两者的异同，从网上搜集了一下资料，主要以下几点</p>
<h4 id="11-使用情况" class="heading-element">
  <a href="#11-%e4%bd%bf%e7%94%a8%e6%83%85%e5%86%b5" class="heading-mark"></a>1.1 使用情况</h4><p>自 1995 年以来，<code>MySQL</code> 一直被视为最广泛使用的开源数据库，而 <code>MariaDB</code> 在最近几年才渐渐得到几个主要巨头比如 <code>Google</code> 和 <code>Linux</code> 社区的赏识，我也是被 <code>ArchLinux wiki</code> 推介从而了解到这个 <code>DBMS</code>。</p>
<h4 id="12-索引结构" class="heading-element">
  <a href="#12-%e7%b4%a2%e5%bc%95%e7%bb%93%e6%9e%84" class="heading-mark"></a>1.2 索引结构</h4><p><code>MariaDB</code> 完全兼容 <code>MySQL</code> 的数据、表格定义、结构和 <code>API</code>，也就是说完全可以平滑的从 <code>MySQL</code> 过渡到 <code>MariaDB</code></p>
<h4 id="13-二进制实现" class="heading-element">
  <a href="#13-%e4%ba%8c%e8%bf%9b%e5%88%b6%e5%ae%9e%e7%8e%b0" class="heading-mark"></a>1.3 二进制实现</h4><p><code>MariaDB</code> 除 <code>C</code>、和 <code>C++</code> 外，还使用 <code>Bash</code> 和 <code>Perl</code></p>
<h4 id="14-复制与集群" class="heading-element">
  <a href="#14-%e5%a4%8d%e5%88%b6%e4%b8%8e%e9%9b%86%e7%be%a4" class="heading-mark"></a>1.4 复制与集群</h4><p><code>MariaDB</code> 和 <code>MySQL</code> 为主终端用户提供与主从主复制和主从复制相同的复制和集群功能。但 <code>MariaDB</code> 还使用 10.1 版以后的 <code>Galera Cluster</code>。</p>
<h4 id="15-支持" class="heading-element">
  <a href="#15-%e6%94%af%e6%8c%81" class="heading-mark"></a>1.5 支持</h4><p><code>MySQL</code> 由 <code>Oracle</code> 的开发人员和工程师支持，<code>MariaDB</code> 由开源社区提供技术支持。</p>
<h4 id="16-安全性" class="heading-element">
  <a href="#16-%e5%ae%89%e5%85%a8%e6%80%a7" class="heading-mark"></a>1.6 安全性</h4><p>就安全性而言，<code>MySQL</code> 为表空间数据提供了强大的加密机制。它提供了强大的安全参数，包括选择好的密码，不给用户不必要的特权，并通过防止<code>SQL</code> 注入和数据损坏来确保应用程序安全。<code>MariaDB</code> 在内部安全和密码检查，验证模块（<code>PAM</code>）和轻量级目录访问协议（<code>LDAP</code>）认证，<code>Kerberos</code>，用户角色以及对表空间，表格和日志的强大加密等安全功能方面取得了重大进展。</p>
<h4 id="17-可扩展性" class="heading-element">
  <a href="#17-%e5%8f%af%e6%89%a9%e5%b1%95%e6%80%a7" class="heading-mark"></a>1.7 可扩展性</h4><p><code>MySQL</code> 不支持，<code>MariaDB</code> 建立在现代架构的基础之上，可以在每一层 &ndash; 客户端，集群，内核和存储上进行扩展。这种可扩展性提供了两个主要优势。它允许通过插件实现持续的社区创新，这意味着可以通过 <code>MariaDB</code> 的可扩展架构集成各种存储引擎，如 <code>MariaDB ColumnStore</code> 或<code>Facebook</code> 的 <code>MyRocks</code>。此外，它使客户能够轻松配置 <code>MariaDB</code> 以支持从联机事务处理（<code>OLTP</code>）到联机分析处理（<code>OLAP</code>）的各种用例。</p>
<h4 id="18-json支持" class="heading-element">
  <a href="#18-json%e6%94%af%e6%8c%81" class="heading-mark"></a>1.8 <code>JSON</code>支持</h4><p><code>MySQL</code> 支持本地 <code>JSON</code> 数据类型，可以在 <code>JSON（JavaScript Object Notation）</code> 文档中高效地访问数据。<code>MariaDB Server 10.2</code> 引入了一整套用于读写 <code>JSON</code> 文档的 24 个函数。另外，<code>JSON_VALID</code> 函数可以与校验约束一起使用，而像 <code>JSON_VALUE</code> 这样的函数可以与动态列一起使用来索引特定的字段。</p>
<h4 id="19-授权许可" class="heading-element">
  <a href="#19-%e6%8e%88%e6%9d%83%e8%ae%b8%e5%8f%af" class="heading-mark"></a>1.9 授权许可</h4><p><code>MySQL</code> 在 <code>GPL</code> 下以开放源代码提供代码，并以 <code>MySQL Enterprise</code> 形式提供非 <code>GPL</code> 商业分发选项。<code>MariaDB</code> 只能使用 <code>GPL</code>，因为它的工作源于该许可条款下的 <code>MySQL</code> 源代码。</p>
<h4 id="110-性能" class="heading-element">
  <a href="#110-%e6%80%a7%e8%83%bd" class="heading-mark"></a>1.10 性能</h4><p><code>MariaDB</code> 通过 <code>MySQL</code> 的许多创新实现了同类最佳性能。其中包括线程池管理以最大限度地提高处理效率，以及 <code>InnoDB</code> 数据存储区内的碎片整理等广泛的优化功能。因此，当从 <code>InnoDB</code> 表中删除行时，可用空间立即可供操作系统使用。不需要将旧表中的数据复制到新表中，并且表空间中没有空闲。<code>MariaDB</code> 还提供与引擎无关的表统计信息，以改善优化程序的性能，加快对表的大小和结构进行查询处理和数据分析。</p>
<h3 id="2-优缺点" class="heading-element">
  <a href="#2-%e4%bc%98%e7%bc%ba%e7%82%b9" class="heading-mark"></a>2 优缺点</h3><h4 id="21-优点" class="heading-element">
  <a href="#21-%e4%bc%98%e7%82%b9" class="heading-mark"></a>2.1 优点</h4><ul>
<li><code>MariaDB</code> 针对性能进行了优化，对于大型数据集，它比 <code>MySQL</code> 强大得多。从其他数据库系统可以优雅的迁移到 <code>MariaDB</code> 是另一个好处。</li>
<li>从 <code>MySQL</code> 切换到 <code>MariaDB</code> 相对容易，这对于系统管理员来说好像是一块蛋糕。</li>
<li><code>MariaDB</code> 通过引入微秒级精度和扩展用户统计数据提供更好的监控。</li>
<li><code>MariaDB</code> 增强了 <code>KILL</code> 命令，使您可以杀死用户的所有查询（<code>KILL USER</code> 用户名）或杀死查询ID（<code>KILL QUERY ID query_id</code>）。<code>MariaDB</code> 也转而使用 <code>Perl</code> 兼容的正则表达式（<code>PCRE</code>），它提供比标准 <code>MySQL</code> 正则表达式支持更强大和更精确的查询。</li>
<li><code>MariaDB</code> 为与磁盘访问，连接操作，子查询，派生表和视图，执行控制甚至解释语句相关的查询应用了许多查询优化。</li>
<li><code>MariaDB</code> 纯粹是开源的，而不是 <code>MySQL</code> 使用的双重授权模式。一些仅适用于 <code>MySQL Enterprise</code> 客户的插件在 <code>MariaDB</code> 中具有等效的开源实现。</li>
<li>与 <code>MySQL</code> 相比，<code>MariaDB</code> 支持更多的引擎（<code>SphinxSE，Aria，FederatedX，TokuDB，Spider，ScaleDB</code>等）。</li>
<li><code>MariaDB</code> 提供了一个用于商业用途的集群数据库，它也支持多主复制。任何人都可以自由使用它，并且不需要依赖 <code>MySQL Enterprise</code> 系统。</li>
</ul>
<h4 id="22-缺点" class="heading-element">
  <a href="#22-%e7%bc%ba%e7%82%b9" class="heading-mark"></a>2.2 缺点</h4><ul>
<li>从版本 5.5.36 开始，<code>MariaDB</code> 无法迁移回 <code>MySQL</code>。</li>
<li>对于 <code>MariaDB</code> 的新版本，相应的库（用于 <code>Debian</code> ）不会及时部署，由于依赖关系，这将导致必需升级到较新的版本。</li>
<li><code>MariaDB</code> 的群集版本不是很稳定。</li>
</ul>
<h3 id="3-开始使用" class="heading-element">
  <a href="#3-%e5%bc%80%e5%a7%8b%e4%bd%bf%e7%94%a8" class="heading-mark"></a>3 开始使用</h3><div class="highlight" id="id-1"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span><span class="lnt">2
</span><span class="lnt">3
</span><span class="lnt">4
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">$ sudo pacman -S mariadb
</span></span><span class="line"><span class="cl">$ sudo mariadb-install-db --user<span class="o">=</span>mysql --basedir<span class="o">=</span>/usr --datadir<span class="o">=</span>/var/lib/mysql <span class="c1"># init dir</span>
</span></span><span class="line"><span class="cl">$ sudo systemctl start mariadb <span class="c1"># or mysqld</span>
</span></span><span class="line"><span class="cl">$ mysql-secure_installation <span class="c1"># install instruction</span></span></span></code></pre></td></tr></table>
</div>
</div><h3 id="参考" class="heading-element">
  <a href="#%e5%8f%82%e8%80%83" class="heading-mark"></a>参考</h3><ul>
<li><a href="https://www.infoq.cn/article/mariadb-vs-mysql"target="_blank" rel="external nofollow noopener noreferrer">MariaDB与MySQL的区别<i class="fa-solid fa-external-link-alt fa-fw fa-xs ms-1 text-secondary" aria-hidden="true"></i></a></li>
</ul>]]></description>
</item>
</channel>
</rss>
